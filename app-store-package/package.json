{
  "name": "recisource",
  "version": "1.1.0",
  "private": true,
  "description": "21 — food, health & fitness, all in one",
  "scripts": {
    "ios:add": "npx cap add ios",
    "ios:sync": "npx cap sync ios",
    "ios:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@perfood/capacitor-healthkit": "^1.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
